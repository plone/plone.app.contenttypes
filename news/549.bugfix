plone.app.contenttypes 2.0.x can be used with Plone 5.1 to support the latest plone.app.mosaic release, but it needs a conditional import on safe_unicode instead of safe_text which is only available in Plone 5.2. [fredvd]